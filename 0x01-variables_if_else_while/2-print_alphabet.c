#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
{
             char j;

             j = ‘a’;
             while (j <= ‘z’)
             {
                    putchar(j);
                    j++;
              }
             putchar('\n'); 
             return (0);
}
