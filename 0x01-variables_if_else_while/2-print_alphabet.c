#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
{
             char j;
             j = ‘a’;
             while (j <= ‘z’)
             {
                    putchar(c);
                    c++;
              }
             putchar('\n'); 
             return (0);
}
